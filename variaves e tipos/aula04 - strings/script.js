/*para concatenar sem o sinal de + podemos usar o método nome.concat() ex:

let nome = "joão"
let sobrenome = "pedro"

nome.concat(sobrenome)
*/

/*
----------------------------------------------------------

*/

/*Split -> separa a string que estiver na variável em vários
 exemplo:

 let frase = "Olá, tudo bem?"
 frase.split("sem espaço")
 ele vai separar cada letra

 (14) ["O","L","á", e assim por diante]

 frase.split("com espaço")
 (3) ["Olá", "tudo", "bem?"]

*/


/*
----------------------------------------------------------

*/

/*Includes -> procuro se existe uma palavra na variável, se tiver vai retornar true
exemplo:

frase.includes("tudo")

resultado

true

*/

/*
----------------------------------------------------------

*/

/*
startsWith("O") -> procuro se começa com a letra O

resultado True

existe o contrário, no final da frase:
frase.endsWith("?"")


*/

/*
----------------------------------------------------------

*/

/*
frase.replace("," , "!")
vai subsituir a vírgula pelo ponto de exclamação.
*/